# File Dossier: shared/core/src/commonMain/kotlin/com/VecturAI/core/loading/BuildingPackageLoader.kt

- **Path**: `shared/core/src/commonMain/kotlin/com/VecturAI/core/loading/BuildingPackageLoader.kt`
- **Type**: source
- **Role**: Handles the deserialization of raw JSON strings into building domain models.
- **Imports / Includes**:
    - `com.VecturAI.core.domain.*`
    - `kotlinx.serialization.json.Json`
- **Exports / Public Surface**:
    - `BuildingPackageLoader` class
    - `load(manifest, navGraph, rooms, markers, rendering)`
    - `loadNavGraph(jsonStr)`
    - `loadRooms(jsonStr)`
    - `loadMarkers(jsonStr)`
- **Main Symbols**:
    - `BuildingPackageLoader`: class
    - `RoomsWrapper`: internal data class for JSON wrapping
    - `MarkersWrapper`: internal data class for JSON wrapping
- **Important Logic by Line Range**:
    - `28-48`: `load` function; deserializes 5 distinct JSON components and aggregates them into a `BuildingPackage`.
    - `53-70`: Specialized loaders for individual components (`NavGraph`, `Rooms`, `Markers`).
- **Uses**:
    - Kotlinx Serialization (Json)
    - Domain models
- **Used By**:
    - `CoreModule` (DI)
- **Config / Constants / Protocol Details**:
    - `ignoreUnknownKeys = true`: Allows for future schema extensions without breaking older app versions.
- **Related Tests**: N/A
- **Notes / Risks**:
    - Assumes the JSON inputs are correctly formatted; throws exceptions on malformed data.
    - Uses internal `RoomsWrapper` and `MarkersWrapper` to match the specific structure of the JSON generated by the preprocessor (top-level `buildingId` key).
