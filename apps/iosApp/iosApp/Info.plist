<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleName</key>
    <string>Vectura AI</string>
    <key>CFBundleDisplayName</key>
    <string>Vectura AI</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UIRequiresFullScreen</key>
    <true/>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arkit</string>
    </array>
    <key>NSCameraUsageDescription</key>
    <string>Vectura AI needs camera access for AR indoor navigation.</string>
    <key>NSMicrophoneUsageDescription</key>
    <string>Vectura AI uses the microphone so you can speak to the route assistant instead of typing.</string>
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>Vectura AI converts your voice into text so the route assistant can understand what you're looking for.</string>
    <key>NSLocalNetworkUsageDescription</key>
    <string>Vectura AI connects to the admin API on your local network for draft management.</string>
    <key>NSBonjourServices</key>
    <array>
        <string>_http._tcp</string>
    </array>
    <key>NSAppTransportSecurity</key>
    <dict>
        <!-- Allow plain HTTP to the dev-only admin API on localhost (simulator). -->
        <key>NSAllowsLocalNetworking</key>
        <true/>
        <!-- Allow plain HTTP to a specific LAN host for real-device testing.
             Change this IP to match your Mac's LAN address when testing on device.
             This entry is intentionally narrow — only the exact host is exempted. -->
        <key>NSExceptionDomains</key>
        <dict>
            <key>192.168.1.18</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
                <key>NSIncludesSubdomains</key>
                <false/>
            </dict>
        </dict>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
</dict>
</plist>
