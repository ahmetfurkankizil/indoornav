1) when navigation process starts, the some arrows displayed behind the camera which seems like navigation has started 1-2 steps ahead of the starting point.

2) After a navigation process is completed whether the route is cancelled or successful arrival, it navigates to destinatio choosing screen which is fine but from there to start second navigation process consequently, it stuck at the waiting for poster screen. Delete that screen completely. 

3) In route preview screen, the minimap at the top does not include the complete map of the selected floor rather it only shows the route, you can modify fetch backend endpoint to get complete map data and to display it properly. 

4) for multi-floor navigation support, when I try to start navigation from a room to another room in different floors, I cannot select the room in another floor in destination selection screen. Try to fix it properly.

5) During AR navigation, it should not display the arrows behind the camera (it should display it in the current camera view), try to fix it properly.

6) In AR navigation, the first arrow should be displayed at the current location of the user, try to fix it properly.

7) In AR navigation, there must be a small minimap at the top right corner to show the users position and the route in the current floor in real time that there must be a cursor indicating the direction and location of the user on the minimap.